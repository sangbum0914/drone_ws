# 작업 로그 — 2026-07-23 14:53

## 작업 개요
드론 시뮬레이션 프로젝트 착수: 아키텍처 설계 확정(codex /align) + Phase 0 부트스트랩(PX4 SITL, ROS2 브리지, estimator 스캐폴드).

## 수정된 파일
- `design/00_overview.md` ~ `phase5_fixedwing.md` — 아키텍처 결정·5단계 로드맵 문서 8종 신규
- `.gitignore` — external/·빌드산출물·데이터 제외
- `ros2_ws/src/drone_estimator/**` — estimator ROS2 패키지 신규(인터페이스·stub·node·evaluator·launch·config)

## 주요 변경사항
- **시뮬레이터 결정**: AirSim-first + PX4 SITL(lockstep) + JSBSim 커스텀기체 + Gazebo fallback. (codex 다라운드 교차검증 합의)
- **PX4-Autopilot**: recursive 클론(2.2GB, HEAD f1bedda) + `ubuntu.sh --no-nuttx --no-sim-tools` 의존성 설치 + `px4_sitl none_iris` 빌드. 스모크 테스트로 "Waiting for simulator on TCP 4560" 도달 확인(빌드 정상).
- **ROS2 브리지**: Micro-XRCE-DDS-Agent 소스 빌드+설치(/usr/local/bin/MicroXRCEAgent), px4_msgs colcon 빌드(261 메시지, VehicleOdometry 포함).
- **estimator 스캐폴드**: `StateEstimatorBase` 추상 인터페이스(교체 지점) + DeadReckoningStub(자리표시자) + estimator_node(표준 메시지 구독→동적 로드 estimator→Odometry 발행, GNSS→ENU 변환) + evaluator_node(ATE RMSE) + launch/config. colcon 빌드·`ros2 run` 구동 검증 완료.

## 이슈 및 특이사항
- **디스크**: 홈 파티션 34GB뿐(99%) → UE5+AirSim(100GB+) 불가. 사용자가 홈 정리 중(현재 36GB). AirSim 설치 전 추가 확보 필요.
- **AirSim/UE5 미설치(블로커)**: Linux UE5는 Epic↔GitHub 연동 필요(사용자 계정). 라이브 토픽 브리지·conformance test(Phase 0 게이트)는 sim 연결 후 수행.
- **setuptools 80 이슈**: console_scripts가 bin/에 설치돼 `ros2 run` 실패 → `setup.cfg`(install_scripts=lib/pkg)로 해결.
- `none_iris`는 빌드+실행 타깃이라 빌드 후 PX4가 sim 대기로 붙잡음(오해 소지) — 검증엔 timeout 스모크로 대체.
